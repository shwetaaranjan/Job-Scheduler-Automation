## Packages
date-fns | Formatting dates for display
framer-motion | Smooth animations for list items and transitions
lucide-react | Icons for UI elements (already in base but explicit check)

## Notes
- Jobs API follows REST conventions at /api/jobs
- Run job endpoint is specific: POST /api/run-job/:id
- Payload field expects valid JSON string
