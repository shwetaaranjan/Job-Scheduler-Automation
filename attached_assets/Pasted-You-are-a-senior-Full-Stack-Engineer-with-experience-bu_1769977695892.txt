You are a senior Full Stack Engineer with experience building production-ready automation systems.

I will paste a detailed assignment reference document and scenario.
Your task is to build a COMPLETE, CLEAN, WELL-STRUCTURED full-stack project that strictly follows the requirements below.

⚠️ IMPORTANT CONSTRAINTS (DO NOT IGNORE):
- Frontend MUST be built using React.js (NOT Next.js)
- Backend MUST be Node.js with Express
- Database MUST be MySQL
- REST APIs only (no GraphQL)
- Use Tailwind CSS for UI styling
- Code must be readable, modular, and production-quality
- Use environment variables for secrets
- Do NOT hardcode credentials
- Follow clean architecture and naming conventions
- Project must be ready to upload to GitHub

────────────────────────────────────
PROJECT REQUIREMENTS
────────────────────────────────────
Build a “Job Scheduler & Automation System” with:

1) CREATE JOB
- Fields: taskName (string), payload (JSON), priority (Low/Medium/High)
- Save job to DB with status = "pending"

2) JOB RUNNER
Endpoint: POST /run-job/:id
- Validate job exists
- Allow execution ONLY if status = "pending"
- Update status → "running"
- Simulate async processing (3 seconds)
- Update status → "completed"
- Save completedAt timestamp
- Trigger outbound webhook on completion

3) DASHBOARD (React UI)
- Table listing all jobs
- Filters by status and priority
- "Run Job" button (only for pending jobs)
- Job detail view showing payload in readable formatted JSON
- Clean, modern UI using Tailwind CSS

4) WEBHOOK INTEGRATION
- Trigger POST request to a configurable WEBHOOK_URL
- Payload must include:
  jobId, taskName, priority, payload, completedAt
- Log webhook request and response

5) APIs REQUIRED
- POST /jobs
- GET /jobs (with optional filters)
- GET /jobs/:id
- POST /run-job/:id
- Optional: POST /webhook-test

6) DATABASE
- MySQL
- jobs table with:
  id, taskName, payload (JSON), priority, status, createdAt, updatedAt, completedAt

────────────────────────────────────
ARCHITECTURE REQUIREMENTS
────────────────────────────────────
Use the following structure:

/frontend
  ├── src/
  │   ├── api/
  │   ├── components/
  │   ├── pages/
  │   ├── App.jsx
  │   └── main.jsx

/backend
  ├── controllers/
  ├── routes/
  ├── services/
  ├── db.js
  ├── app.js
  └── .env.example

Include:
- Clear separation of concerns
- Reusable React components
- Clean Express routing
- Proper error handling
- Input validation
- Async-safe job execution

────────────────────────────────────
DELIVERABLES
────────────────────────────────────
1) Complete backend code
2) Complete React frontend code
3) MySQL schema SQL file
4) README.md including:
   - Setup instructions
   - Architecture explanation
   - API documentation
   - Webhook explanation
   - AI usage disclosure section

────────────────────────────────────
IMPORTANT OUTPUT RULES
────────────────────────────────────
- Generate FULL files, not snippets
- Mention file paths before each code block
- Do NOT skip any required feature
- Assume the project will be reviewed by a senior engineer
- Follow best practices over shortcuts

────────────────────────────────────
NOW WAIT.
I WILL PASTE THE ASSIGNMENT DOCUMENT NEXT.
DO NOT GENERATE CODE UNTIL I SAY:
"START BUILDING THE PROJECT"
